<input type="text" class="form-control {{if isMobile 'bs-disable'}}" placeholder={{placeholder}} onfocus={{action 'focus'}}>
{{#if showIcon}}
  <span class="input-group-addon">
    <i class={{iconClasses}}></i>
  </span>
{{/if}}
